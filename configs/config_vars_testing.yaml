worker-threads: 4
worker-fs-threads: 2
logger-level: debug

is_testing: true

server-port: 8080

# dummy_bookstore_book_shop is the service name + _ + filename of the book_shop.sql
dbconnection: 'postgresql://testsuite@localhost:15433/dummy_bookstore_book_shop'
